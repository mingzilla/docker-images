## Usage

You can now copy these 2 files to any directory on your machine:

- scripts_sh/embed.sh
- scripts_sh/embedding_config.json

In any folder with these files:

1. Place your `input.duckdb` file
2. Edit `embedding_config.json` to match your table structure
3. Run: `./embed.sh`
4. Get `output.duckdb` in the same directory